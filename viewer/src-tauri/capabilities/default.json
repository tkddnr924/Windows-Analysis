{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core + dialog permissions for the viewer window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
