# Rust build output (rust/target + viewer/src-tauri/target)
target/

# Parsed case output — real extracted forensic data (browser data, account
# fields, etc.); never belongs in version control.
cases/

# The collected sample under analysis — never commit.
Target/

# Node / Next / Tauri
viewer/node_modules/
viewer/.next/
viewer/out/
viewer/src-tauri/gen/
viewer/src-tauri/binaries/

# Editor / OS / local
.claude/
.DS_Store
__pycache__/
*.tsbuildinfo
